# LowVol 低波动策略

## 策略概述

**低波动防守策略 - 熊市专用**

### 策略定位

| 市场环境 | 策略角色 | 预期表现 |
|----------|----------|----------|
| **熊市** | 🛡️ 主力防守 | 减少亏损，最大回撤 < -10% |
| **震荡市** | ⚖️ 稳健配置 | 小幅盈利，超越基准 |
| **牛市** | 🚫 降低仓位 | 不追求收益，减少拖累 |

### 核心因子

| 因子 | 权重 | 描述 |
|------|------|------|
| **波动率** | 40% | 20日波动率，越低越好 |
| **ATR** | 25% | 14日 ATR 过滤 |
| **Beta** | 20% | 对冲市场Beta，越低越好 |
| **质量** | 15% | ROE > 10%，过滤垃圾股 |

## 目录结构

```
lowvol_strategy/
├── __init__.py          # 主入口
├── config/               # 配置模块
│   └── __init__.py
├── factors/             # 因子模块
│   └── __init__.py      # 波动率、ATR、Beta、质量因子
├── selection/           # 选股模块
│   └── __init__.py
├── position/            # 仓位管理
│   └── __init__.py
├── backtest/           # 回测模块
│   └── __init__.py
└── tests/               # 测试模块
    └── __init__.py
```

## 使用方法

```python
from lowvol_strategy import LowVolSelector, LowVolConfig

# 创建配置
config = LowVolConfig({
    'market_type': 'bear',
    'top_n': 30,
})

# 创建选股器
selector = LowVolSelector(config)

# 选股
selected = selector.select(data, market_type='bear')

# 获取仓位
position = selector.get_position('bear')
```

## 回测

```python
from lowvol_strategy import LowVolBacktest

bt = LowVolBacktest({
    'initial_capital': 100000,
    'market_type': 'bear',
    'position_size': 0.4,
})

result = bt.run(data)
print(result)
```

## 预期表现

### 熊市环境

| 指标 | 目标 |
|------|------|
| **年化收益** | 5-10% |
| **最大回撤** | < -10% |
| **夏普比率** | > 0.8 |
| **跑赢基准概率** | > 70% |

---

*版本: 1.0*
